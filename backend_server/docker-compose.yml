version: '3.8'
services:
  api:
    build: .
    container_name: videosaver-api
    restart: unless-stopped
    ports: ["8000:8000"]
    volumes: ["./downloads:/app/downloads","./temp:/app/temp"]
    environment: [HOST=0.0.0.0,PORT=8000]
